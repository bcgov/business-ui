---
"@sbc-connect/nuxt-business-base": patch
---

ManageShareStructure - show the resolution/court order date input in correction filings any time collection is enabled, not only when share classes were changed in the current session.
